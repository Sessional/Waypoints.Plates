Supports multiverse multiple world teleports

Allows pressure plates to be used to teleport.
Possible locations must be adjacent to the tile on the same level, immediately above the block, or below the block the pressure plate is on.

Permissions are:
Either of these permissions will enable the use of pressure plate teleporting.
waypoints.go
waypoints.plates